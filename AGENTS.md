# Repository Instructions

For any thesis-related work, inspect `.memory/README.md` first.

That directory contains persistent project memory about IME-USP workflow, defense metadata, editorial conventions, and repository-specific decisions.

When facts change, update both `.memory/` and the relevant LaTeX/config sources.
